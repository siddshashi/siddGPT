// linear affine transformation
pub struct Linear {
    weight: Array2<f32>,
    bias: Array1<f32>,
}

impl Linear {
    // create method, initializes with He? takes in d_in, d_out

    // forward method, applies Wx + b
}
